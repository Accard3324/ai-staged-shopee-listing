# Changelog

All notable public changes will be recorded here. Dates use `YYYY-MM-DD` when a release date has been confirmed.

## [Unreleased]

## [0.1.0]

- First public source release of the AI-staging/deterministic-execution workflow under GitHub account `vip-jiekaixu`.
- Prepare a sanitized open-source release with governance, provenance, security, CI, and Codex for Open Source maintenance guidance.
- Add OpenAI API support and make `gpt-5.6` the default model for every AI stage.
- Make multimodal mode the Step 0 default.
- Convert the operator interface, prompts, documentation, and runtime messages to English.
- Keep asset loading manual-only and mark automatic asset download as planned.
- Sanitize the release so it contains no populated `.env` file or real API key.
- Clear the public starter prompts for Steps 3, 5, 6, and 7 and clear every bundled store-description template.
- Let Step 0 add and persist custom store names while reusing the selected store's workbook status-column and write-back-sheet mapping.
- AI-staged listing workflow with per-image objective records and separate selection assessments.
- Editable keyword, competitor-title, final-title, and description stages.
- Structured listing draft and resumable artifacts.
- Ziniao CDP discovery and deterministic Shopee Step 1/Step 2 form components.
- Pre-save checklist, Save and Delist, unlisted-result verification, and workbook write-back.
- Offline regression suite covering workflow, browser components, AI schemas, archive handling, reporting, and failure gates.

The authoritative public disclosure date is the timestamp of the first public GitHub commit and `v0.1.0` release.
